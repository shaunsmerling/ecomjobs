export default function posts() {

    // Display all jobs, then jobs.map((job) => {
        // if {user.id} === {job.user_id} {
            // return job 
      //  }
    // }) 

    return (

        <div className="text-center"> 
            <h1 className="mb-10">My Jobs 💼</h1>
            <p>You have not posted any jobs yet.</p> 
        </div>
    )
}